/**
 * Prod origin of the FREEPORT.com web app (marketing site + browser sign-in).
 * The CLI and FREEPORT Desktop both send users here for the device-code login
 * flow; sharing the literal keeps their prod defaults from drifting.
 */
export const FREEPORT_WEB_URL_PROD = 'https://FREEPORT.com'
